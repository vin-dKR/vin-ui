---
title: Random Emoji
date: 2023-08-02
description: Cool random-emoji transition 
published: true
---

<ComponentPreview name="random-emoji" />

# Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
vin-ui add random-emoji
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install cobe
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="random-emoji" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

# Props
| Prop | Type | Default | Description |
|------|------|---------|-------------|
| (All standard `span` props) | `React.ComponentProps<'span'>` | - | Accepts all standard HTML span element attributes |

# Behavior
- Changes the displayed emoji every 500ms
- Avoids showing the same emoji consecutively
- Cleans up the interval timer on unmount
- Accepts all standard span props (className, style, etc.)



# Example Usage

```jsx
<RandomEmoji 
  className="text-2xl"
  aria-label="Randomly changing emoji"
/>
